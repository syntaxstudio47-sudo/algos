//@version=5
strategy("Premarket High Breakout", overlay=true, pyramiding=0)
len = input.int(20, "Premarket Length")
atrLen = input.int(14, "ATR Length")
atrMult = input.float(1.3, "SL ATR Mult")
rr = input.float(2.5, "RR")
atr = ta.atr(atrLen)
pHigh = ta.highest(high, len)[1]
pLow = ta.lowest(low, len)[1]
longCond = ta.crossover(close, pHigh)
shortCond = ta.crossunder(close, pLow)
if longCond
    strategy.entry("L", strategy.long)
    strategy.exit("LX", "L", stop=close-atr*atrMult, limit=close+atr*atrMult*rr)
if shortCond
    strategy.entry("S", strategy.short)
    strategy.exit("SX", "S", stop=close+atr*atrMult, limit=close-atr*atrMult*rr)
